[project]
name = "stadium-crisis-simulator"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn",
    "python-dotenv",
    "google-genai",
    "python-multipart",
]

[tool.vercel]
entrypoint = "backend.main:app"